/**
 * Handles the communication between EC2 server and a client
 */
public class EC2Protocol {

	public String processInput(String input) {
		String output = null;
		if (input.startsWith("put")) {
			
		}
		if (input.startsWith("get")) {
			
		}
		return output;
	}
	
}
